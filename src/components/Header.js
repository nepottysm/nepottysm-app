import React from 'react';
import logo from '../noun_poop.svg';

const Header = () => (
  <header className="App-header">
    <div className="heading">
      <img src={logo} className="App-logo" alt="logo" />
      <h2>Nepottysm</h2>
    </div>
  </header>
);

export default Header;
